#include <stdio.h>

int main () {
    // declaracao de variaveis e processamento junto
    int divisao = 100000 % 3;
    // exibicao do resultado
    printf("O resto da divisão de 100000 por 3 é: %d\n", divisao);
    
    return 0;
}